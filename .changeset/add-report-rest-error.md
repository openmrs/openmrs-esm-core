---
"@openmrs/esm-error-handling": minor
---

Added `reportRestError` — a new public utility that parses structured OpenMRS REST
API error responses and surfaces them to the user via a snackbar notification.
